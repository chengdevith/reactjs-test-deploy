# set condition
on:
  push:
    branches: ["master"]

env:
  DOCKER_IMAGE_NAME: gitops
  HELM_REPO: https://github.com/chengdevith/reactjs-chart.git
# build docker image and push it into docker hub
jobs:
  # ci
  ci-job:
    runs-on: ubuntu-latest
    environment: DOCKER_ENV
    steps: 
      - name: Checkout Code
        uses: actions/checkout@v3
      - name: Using commit SHA
        run: |
          echo "TAG=${GITHUB_SHA::8}" >> $GITHUB_ENV
      - name: Show the SHA values
        run: |
          echo "TAG = ${{ env.TAG }}"
      - name: Docker login
        run: |
          echo "${{ secrets.TOKEN }}" | docker login -u ${{ secrets.USERNAME }} --password-stdin
      - name: Build & tag 
        run: |
          docker build -t ${{ secrets.USERNAME}}/${{ env.DOCKER_IMAGE_NAME }}:${{ env.TAG }} .
      - name: Push to dockerhub
        run: |
          docker push ${{ secrets.USERNAME}}/${{ env.DOCKER_IMAGE_NAME }}:${{ env.TAG }} 
      - name: Log from dockerhub
        if: always()
        run: | 
          docker logout
  # cd
  cd-job:
    needs: ci-job
    runs-on: ubuntu-latest
    steps:
      - name: Add commit SHA to tag env
        run: |
          echo "TAG=${GITHUB_SHA::8}" >>$GITHUB_ENV
      - name: Clone argocd repo
        run: |
          git clone ${{ env.HELM_REPO }} helm_repo
      - name: Update helm chart
        run: |
          cd helm_repo

          echo "Update repository username/image_name"

          yq -i ".image.repository=\"${{ secrets.USERNAME }}\"" values.yaml

          echo "update image tag"

          yq -i ".image.tag=\"${{ env.TAG }}\"" values.yaml

          git config --global user.email "chengdevith5@gmail.com"
          git config --global user.name "chengdevith"
          git add .
          git commit -m "update helm to use image ${{ env.TAG}}"

          git push https://${{ secrets.GIT_TOKEN }}@github.com/${{ env.HELM_REPO }}

      - name: Clean up
        run: |
          rm -rf helm_repo